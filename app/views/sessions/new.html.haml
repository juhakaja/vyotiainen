.sign
  %h1 Login

  = form_for(:session, url: sessions_path) do |f|

    = f.label :email
    = f.text_field :email

    = f.label :password
    = f.password_field :password

    = f.submit "login", class: "btn btn-large btn-primary"

  = link_to "Don't have an account? Create one here.", create_account_path
